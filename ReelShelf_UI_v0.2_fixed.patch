--- a/lib/screens/shell_screen.dart
+++ b/lib/screens/shell_screen.dart
@@ -45,6 +45,12 @@
         actions: [
           if (_index != 2)
             IconButton(
+              tooltip: _index == 0 ? 'Film hinzufügen' : 'Film zur Wunschliste',
+              onPressed: () => _openAdd(),
+              icon: const Icon(Icons.add_rounded),
+            ),
+          if (_index != 2)
+            IconButton(
               tooltip: 'Barcode scannen',
               onPressed: _scanBarcode,
               icon: const Icon(Icons.qr_code_scanner_rounded),
@@ -69,13 +75,6 @@
               ],
             )
           : const Center(child: CircularProgressIndicator()),
-      floatingActionButton: _index == 2
-          ? null
-          : FloatingActionButton.extended(
-              onPressed: _openAdd,
-              icon: const Icon(Icons.add_rounded),
-              label: const Text('Film'),
-            ),
       bottomNavigationBar: NavigationBar(
         selectedIndex: _index,
         onDestinationSelected: (value) => setState(() => _index = value),
--- a/lib/widgets/collection_card.dart
+++ b/lib/widgets/collection_card.dart
@@ -46,7 +46,7 @@
           const SizedBox(height: 9),
           Text(
             item.title,
-            maxLines: 2,
+            maxLines: 1,
             overflow: TextOverflow.ellipsis,
             style: const TextStyle(
               fontWeight: FontWeight.w800,
